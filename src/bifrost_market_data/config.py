"""YAML configuration loading for market-data workers (implemented in later phases)."""

from __future__ import annotations

from typing import Any


def load_config(path: str | None = None) -> dict[str, Any]:
    """Load market-data YAML config. Stub for P0 — implemented in later phases."""
    _ = path
    return {}
